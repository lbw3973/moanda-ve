import Swiper from "../components/Swiper";
import * as S from "./style";

const Home = () => {
  return (
    <section>
      <Swiper />
      <S.tempDIV>Home</S.tempDIV>
    </section>
  );
};

export default Home;
